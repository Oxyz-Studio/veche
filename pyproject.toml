[project]
name = "veche"
version = "0.1.0"
description = "Cooperative consensus consolidation of noisy GUI observations"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
testpaths = ["tests"]
