# Secrets — NEVER commit
.env
.env.local
*.key

# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node
node_modules/

# Data / scratch
*.log
data/
captures/
recordings/

# Internal strategy notes (kept local, not published)
veche.md
veche-spec.md

# demo assets (frames + videos) ARE committed so the demo runs straight from a clone
viz/recording/recording_salvage.json
viz/recording/videos/*.webm
